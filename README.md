This is my submission for the react project. To run the site open command prompt and change the directory to the folder containing the source code. Next run the command "npm run dev" and control+click the link shown (http://localhost:5173).
